info:
  name: get clients1
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3001/api/clients
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
