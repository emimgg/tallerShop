info:
  name: post user
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3001/api/auth/register
  body:
    type: json
    data: |-
      {
        "name": "emi",
        "email": "emi@email.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
