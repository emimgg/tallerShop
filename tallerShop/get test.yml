info:
  name: get test
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/inventory
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
