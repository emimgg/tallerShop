info:
  name: post client
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3001/api/clients
  body:
    type: json
    data: |-
      {
        "name": "John Doe",
        "phone": "0981123456",
        "email": "john@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
